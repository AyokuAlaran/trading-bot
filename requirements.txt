# Polymarket Trading Bot — Python Dependencies
# Install with:  pip install -r requirements.txt

# Anthropic Python SDK — Claude API (analytical bot)
anthropic>=0.40.0

# Supabase Python client — all state storage (replaces trades.json)
supabase>=2.0.0

# HTTP client — Polymarket Gamma API
requests>=2.31.0

# RSS feed parsing — sniper news monitor
feedparser>=6.0.0

# Environment variable loading from .env
python-dotenv>=1.0.0

# Flask — health server required by Render free web-service tier
flask>=3.0.0

# Polymarket CLOB client — real order execution
py-clob-client>=0.17.0
